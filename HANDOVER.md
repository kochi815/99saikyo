# 九九最強王 ポケモンバトル — 引き継ぎメモ

小学2年生が九九を楽しく覚えるためのブラウザゲーム。`index.html` をブラウザで開くだけで動く（ビルド不要、オフライン可、localStorage保存）。

## 遊び方（コアループ）
- 1問＝1ターン。「7 × 6 = ?」を4択（モンスターボール型ボタン）で答える
- **正解** → ピカチュウが攻撃（100ダメージ）。**3秒以内**ならクリティカル150＋「はやい！」
- **不正解／時間切れ** → 敵の攻撃でハート−1 → 正解が光り唱え読み「しちろく しじゅうに」表示 → **光った正解をタップして続行**（誤り訂正入力）
- 間違えた問題は **2〜3問後に必ず再出題**（リベンジ。正解でクリティカル扱い）
- 同じ問題を2回連続で間違えると次回は **2択＋ヒント＋ドット図** に
- 5問正解でゲージMAX → 次の正解で **10まんボルト（300）**。コンボが切れてもゲージは残る
- 敵HP 0で勝利 → 捕獲演出 → 図鑑登録 → 星評価（ミス1以下＝★★、ノーミス＆速答率60%以上＝★★★）

## ステージ構成（config.js `stages`）
| ワールド | 内容 | 制限時間 |
|---|---|---|
| W1 だんバッジロード | 1→5→2→3→4→6→7→8→9の段（学校の学習順）＋ジムボス（ゲンガー・選択肢シャッフル） | なし。未クリア面は開始前に「おぼえタイム」（段の表が流れる） |
| W2 ミックスコロシアム | 2&5 / 3&4 / 2〜5 / 6&7連戦 / 7&8（ソーナンスのカウンター） / 8&9（ギャラドス2形態） | 10秒 |
| W3 でんせつロード | 1〜5 / 6〜9連戦 / **にがてリベンジ（自分の苦手TOP12）** / チャンピオン3連戦（ミュウツーはクリティカルのみ有効） | 8秒〜5秒 |
| 裏 まぼろしの島 | 全81問・**1問3秒**・ハート3・ミュウ。解禁＝チャンピオン撃破＋くくマップ銀60マス | 3秒 |

常設: **メタモンとっくん**（苦手TOP5のミニバトル、日替わり1回でクリア回数がきせかえ解放に）、**くくマップ**（81マス習熟ヒートマップ、タップで1問練習）

## ファイル構成
| ファイル | 役割 |
|---|---|
| index.html | 全画面DOM。onclick属性は使わず main.js で addEventListener 集中登録 |
| config.js | ステージ・敵・唱え読み81個・図鑑・きせかえ・月替わりタイトル画像 |
| state.js | GameState: 81ファクト統計 `facts["7x6"]={n,c,w,f,s,cw}`、進行、図鑑、きせかえ、設定 |
| storage.js | localStorage `kuku_saikyoou_save_v1`（save/loadのフィールドは state.js 冒頭コメント参照） |
| questions.js | 出題（苦手重み付き抽選）＋誤答肢生成（隣の九九 a×(b±1),(a±1)×b を2つ＋近くにある他の九九の答えを1つ。±1/±10は最終フォールバック） |
| battle.js | バトル進行。**全setTimeoutは `_later()` 経由**（`_session` トークンで「にげる」後の残留発火を無効化） |
| ui.js | HPバー・ハート・コンボ・ゲージ・タイマー・実況・問題描画 |
| screens.js | タイトル/マップ/リザルト/エンディング/くくマップ/ずかん/きせかえ/せってい/学習レポート |
| sound.js | mp3 SE ＋ wav BGM（ポケモン風）＋ WebAudioコンボ音（ド→レ→ミ…） |
| effects.js/.css, transition.js/.css | 前作「算数最強王」から流用（バトルエフェクト・画面遷移） |
| img/ | ポケモンGIF素材 | assets/sounds/ | 音素材（wavは**NFC正規化済み**ファイル名。Mac由来のNFD名だとブラウザから読めない） |

## 実装規約（前作の教訓）
1. 画面ごとのID接頭辞（`battle-` / `map-` / `kuku-` / `dex-` / `dress-` / `settings-` / `report-`）
2. 画面遷移は必ず `TransitionManager.fade / battleWipe` 経由
3. バトル内の遅延処理は `BattleManager._later(ms, fn)` を使う（素の setTimeout 禁止）
4. CSSアニメ再発火は `classList.remove → void el.offsetWidth → classList.add`
5. 永続化フィールドを増やしたら storage.js の save/load 両方に追加

## 保護者向け
せってい → 「おうちのひとへ」で段別正答率・苦手ワースト10・マスター状況を確認できる。

## 調整履歴
- 2026-08-15 v1.1: 敵HP約+25%（W1ジム 600→800〜1000、ボス1000→1300 等）／誤答肢を「隣の九九・近くの九九の答え」優先に／敵が2戦目以降透明になるバグ修正（`enemy-defeated` クラスの残留）／小画面(375×667)対応（テロップ幅・位置、フィールド高さ可変、2択ヒントの縦積み）／勝利確定後の「にげる」無効化／再戦は勝利・敗北画面から直接ワイプ
